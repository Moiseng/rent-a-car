{% extends 'base.html.twig' %}

{% block body %}
    <div class="section no-pad-bot template" id="index-banner">
        <div class="container">
            <br><br>
            <h1 class="header center  orange-text text-darken-3">Rent a car</h1>
            <div class="row center">
                <h5 class="header col s12 light">Location de voitures</h5>
            </div>
            <div class="row center">
                <a href="{{ path('app_index_add') }}" class="btn-large waves-effect waves-light orange darken-3">Ajout</a>
                <a href="{{ path('app_search_searchcar') }}" class="btn-large waves-effect waves-light orange darken-3">Recherche avancée</a>
            </div>
            <br><br>

        </div>
    </div>

    <div class="container">
        <!--   Icon Section   -->
        <div class="row center">
            <h2>voitures disponibles:</h2>
                <ul class="center">
                    {% for car in cars %}
                        <li>
                            <h3>
                                <a href="{{ path('app_index_show', {'id': car.id}) }}">{{ car.model }}</a>
                                <a href="{{ path('app_index_edit', {'id': car.id}) }}">Modifier</a>
                            </h3>
                        </li>
                    {% endfor %}
                </ul>
        </div>
    </div>
{% endblock %}